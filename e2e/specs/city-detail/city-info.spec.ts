import { test, expect } from "../../fixtures";
import { CityDetailPage } from "../../pages/CityDetailPage";

test.describe("도시 상세 — 기본 정보", () => {
  test("도시 이름이 페이지에 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("도시 설명 텍스트가 렌더링된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("도시 이미지가 로드된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("존재하지 않는 슬러그 접근 시 404 페이지가 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  // suppress unused import warning
  void CityDetailPage;
  void expect;
});

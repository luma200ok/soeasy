import { test, expect } from "../../fixtures";
import { HomePage } from "../../pages/HomePage";

test.describe("홈 — 필터 바", () => {
  test("예산 필터 변경 시 해당 조건의 도시 카드만 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("지역 필터 변경 시 해당 지역 도시 카드만 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("환경 필터 변경 시 해당 환경 도시 카드만 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("시즌 필터 변경 시 해당 시즌 도시 카드만 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("필터 조합 적용 시 교집합 결과가 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("필터를 초기화하면 전체 도시 카드가 다시 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  // suppress unused import warning
  void HomePage;
  void expect;
});

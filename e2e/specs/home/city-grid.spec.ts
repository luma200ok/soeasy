import { test, expect } from "../../fixtures";
import { HomePage } from "../../pages/HomePage";

test.describe("홈 — 도시 카드 그리드", () => {
  test("홈 페이지 접속 시 도시 카드 목록이 렌더링된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("도시 카드를 클릭하면 해당 도시 상세 페이지로 이동한다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("더보기 버튼 클릭 시 추가 도시 카드가 로드된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  test("초기 렌더링 시 일정 개수 이상의 카드가 표시된다", async ({ page }) => {
    // TODO: 구현
    void page;
  });

  // suppress unused import warning
  void HomePage;
  void expect;
});
